# Copy this file to .env and fill in real values.
# NEVER commit the actual .env file.

# Redis auth password — required, must not be blank
REDIS_PASSWORD=strong-password

# Shared queue name (must match across api + worker)
QUEUE_NAME=jobs
