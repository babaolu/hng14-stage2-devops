[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=. --cov-report=xml --cov-report=term-missing"

[tool.coverage.run]
source = ["."]
omit = ["tests/*"]

[tool.coverage.report]
omit = ["tests/*"]
